---
title: "Table_test"
author: test
date: "16 10 2019"
output: word_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(gridExtra)
```


```{r}

par(mfrow=c(1,2))
    plot(cars)
    plot(cars)
    
    
```


## R Markdown

bla    | blub   |     
-------|--------|-------
Cell 1 | `r Sys.time()` | Cell 3
<br> | Cell 2 | Cell 3

\alpha

$I(t) = \sum_{k = 1}^{K} n_k\lambda_k\exp{-\lambda_kt}$


$$I(t) = \sum_{k = 1}^{K} n_k\lambda_k\exp{(-\lambda_kt)}$$

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

\pagebreak

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}

A <- data.frame(a = c(1,2), b = c("hello world","hello human"))
colnames(A) <- c("$x^3$", "$\\lambda_3$")

knitr::kable(A, booktaps = TRUE,  escape = FALSE, align = 'c',
             caption = "**table 1:** Hey ho")
```
